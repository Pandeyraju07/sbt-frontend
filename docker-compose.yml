services:
  sbt-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sbt-frontend-app
    ports:
      - '3000:80'
    environment:
      - VITE_API_BASE_URL=http://localhost:8080/api
      - VITE_APP_ENV=production
    restart: unless-stopped
